<Window x:Class="PulseAgent.Ui.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pulse — Device Setup" Height="560" Width="440"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource Bg}" Foreground="{StaticResource Text}"
        FontFamily="Segoe UI">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
            <Border Background="{StaticResource Brand}" CornerRadius="6" Width="30" Height="30">
                <TextBlock Text="◉" Foreground="White" FontSize="16"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock Text="Pulse" FontSize="22" FontWeight="SemiBold" Margin="10,0,0,0"
                       VerticalAlignment="Center"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="Connect this device to your Pulse server."
                   Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,18"/>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Server address" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,4"/>
            <TextBox x:Name="ServerBox" />

            <Grid Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Port" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,4"/>
                    <TextBox x:Name="PortBox" Width="90" HorizontalAlignment="Left"/>
                </StackPanel>
                <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                    <CheckBox x:Name="HttpsBox" Content="Use HTTPS" Foreground="{StaticResource Text}" Margin="0,0,0,6"/>
                    <CheckBox x:Name="InsecureBox" Content="Allow self-signed cert" Foreground="{StaticResource Muted}"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="Enrollment key" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,14,0,4"/>
            <TextBox x:Name="KeyBox" />

            <TextBlock Text="Device name" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,14,0,4"/>
            <TextBox x:Name="NameBox" />
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="3" TextWrapping="Wrap" Margin="0,16,0,12"
                   FontSize="12" Foreground="{StaticResource Muted}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Cancel" Click="OnCancel"
                    Padding="16,8" Margin="0,0,10,0" Background="{StaticResource Surface}"
                    Foreground="{StaticResource Text}" BorderBrush="{StaticResource Border}"/>
            <Button x:Name="EnrollButton" Content="Enroll &amp; Start" Click="OnEnroll"
                    Padding="18,8" Background="{StaticResource Brand}" Foreground="White"
                    BorderThickness="0" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
